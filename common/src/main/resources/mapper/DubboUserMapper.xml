<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper.DubboUserMapper">

    <select id="getUser" resultType="entity.DubboUserEntity">
        select * from dubbo_user where us_id = #{usId}
    </select>

</mapper>
